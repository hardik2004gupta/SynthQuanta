# SynthQuanta backend — Phase 1 + Phase 2 + Phase 3 dependencies
# Install with: pip install -r requirements.txt

# API framework
fastapi[standard]>=0.115
uvicorn[standard]>=0.30

# Configuration
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# Database
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9

# Numerical / data engine (Phase 2)
numpy>=1.26
scipy>=1.13

# ML / training (Phase 3)
torch>=2.1

# HTTP client (used in tests)
httpx>=0.27

# Testing
pytest>=8.0
pytest-asyncio>=0.23
anyio>=4.0
